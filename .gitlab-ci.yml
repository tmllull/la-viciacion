variables:
  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar" # Defines the location of the analysis task cache
  GIT_DEPTH: "0" # Tells git to fetch all the branches of the project, required by the analysis task

# stages:
#   - sonar
# - build api main
# - build api develop

sonarcloud-check:
  image:
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  stage: sonar
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - sonar-scanner
  only:
    - main
    - develop
# build:
#   image: docker:20.10.16
#   stage: build api main
#   services:
#     - docker:20.10.16-dind
#   script:
#     - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
#     - docker build -t $CI_REGISTRY/tmllull/la-viciacion:latest -f api/Dockerfile ./api
#     - docker push $CI_REGISTRY/tmllull/la-viciacion:latest
#   only:
#     - main

# build:
#   image: docker:20.10.16
#   stage: build api develop
#   services:
#     - docker:20.10.16-dind
#   script:
#     - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
#     - docker build -t $CI_REGISTRY/tmllull/la-viciacion:develop -f api/Dockerfile ./api
#     - docker push $CI_REGISTRY/tmllull/la-viciacion:develop
#   only:
#     - develop
